# R19.2 固定股票池＋免費價量研究
以 r18 ad5d249cbcc0534741574b03442ac1134e8901c8 為基底，包含 R19/R19.1 更新；不變更正式 V4.4 分數。

上傳更新 ZIP 後開啟 /strategy-regression-lab.html。
1. 選固定股票池、純價量假說版。
2. 載入目前股票期貨標的並核對；也可手動輸入 twse:2330 tpex:6488 等市場:代碼。名單取得日期不表示歷史成分生效日。名單設定會保留在同一瀏覽器。
3. 補齊歷史資料，再搜尋與樣本外驗證。可停止續接；請匯出資料備份與完整研究結果。

固定池行情沿用免費 history/auto (Yahoo / TWSE)，不呼叫 FinMind；純價量跳過全部籌碼。原版 V4.4 模式仍要求籌碼完整。逐日 top100 模式保留，其全市場來源限制仍適用。
固定池歷史以代碼與期間快取，換期間需重新抓取；同期間本機搜尋零上游。初次固定池約每檔一次歷史 API 加一份 0050 日曆代理，來源重試可能增加外部請求。不等於每天或每個參數再抓一次。

價量假說固定公式：clamp 為限制到 0..100；T=clamp(50+500*(MA20/MA60-1)), M=clamp(50+500*(C/C[-5]-1)), B=clamp(50+500*(C/前20根最高價-1)), V=clamp(50+25*log2(當日量/前20日均量))。Setup=T, Opportunity=(M+V)/2, Entry=B/2+(M+V)/4, Hold=clamp(50+500*(C/MA20-1))。只使用當天以前資料，90根暖機，非原版 V4.4。再搜尋四分數及每日變化的進出場權重／門檻。
55/20/25 時序切分、下一交易日開盤、費用稅滑價、26日退出緩衝沿用 R19。研究報告保存固定名單與取得日期、分數版本、樣本缺漏。單日少於20個有效股票不計截面 IC，顯示空值而非零。

限制：今日股期名單回測歷史有存活者/選樣偏差，不是歷史 point-in-time 股票池。股期名單沿用 r18 官方 HTML 解析（需人工核對），遇無法市場分類停止；不靜默遞補。研究是股票現貨，不是期貨合約，沒有保證金或轉倉。免費行情可能限流或無資料，不保證每個部署都可連線；失敗停止且保留完成快取。0050 日曆為代理，若代理缺交易日仍可能影響成交時間，正式驗證需官方交易日曆。缺個股 K 線不合成價格，不能成交就依 R19 規則處理。未計股息／除權調整／最低手續費／資金上限，不能當正式投資組合績效。

驗證：node --test tests/research*.test.cjs。38 tests passed，含真實 Worker 兩種分數流程、價量缺籌碼可執行、未來資料不改寫過去分數、固定池缺價不成交、固定池本機 UI 零網路。使用合成行情驗證機制，尚未完成真實全股票池績效驗證；不能宣稱策略有效。
