# V1.11.2 — 按鈕完整回復 + 盤中模組隔離

V1.11.0 / 1.11.1 的盤中程式直接修改 Detail 主 JavaScript，造成既有按鈕初始化受到影響。

本版不再修補那份程式，而是直接回到已知可正常操作的 V1.10.0 Detail 主程式。

盤中功能重新設計成第二個完全獨立的 script：
- 不修改 analyze / latest / watch / back / slider 的 handler。
- 不新增任何既有按鈕事件。
- 即使盤中 API 整段失敗，也只 console.warn，不可能中止 V1.10 主程式。
- 今日資訊仍顯示現價、漲跌、今開、最高、最低、盤中量、更新時間。
- 正式四分數仍只使用最後完整交易日。

這版先取消「盤中四分數試算按鈕」，優先把所有原本按鈕恢復到 V1.10.0 的穩定狀態。
