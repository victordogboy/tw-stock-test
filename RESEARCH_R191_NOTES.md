# R19.1：修正行情來源安全性阻擋的處理

此更新承接 R19，仍以 r18 原始正式引擎為基底。只提供 ZIP 由使用者上傳，未寫入 GitHub。

## 你看到的畫面代表什麼

R19 頁面已部署成功。`Upstream 307` 後面的「因為安全性考量／FOR SECURITY REASONS」來自行情來源，表示來源拒絕伺服器存取。這不是股票資料為零，也不是重跑回測能解決的問題。

R19.1 不會解除來源的安全性限制。它會辨識阻擋頁、停止同一來源的替代路徑重試、暫停 10 分鐘，改顯示市場、日期與可用的下一步。成功行情與籌碼快取保留；阻擋頁不會當成成功資料或休市日。

## 更新後怎麼操作

1. 將 `update-v1.17.0-r19.1-source-options.zip` 原檔上傳 repository 根目錄，等既有流程部署完成。
2. 重開同一個 `/strategy-regression-lab.html`；標題應為 R19.1。
3. 「歷史行情來源」可選：
   - **交易所官方**：沿用官方歷史行情；遭來源封鎖時不能保證可用。
   - **FinMind（需全市場權限）**：使用該服務授權的 `TaiwanStockPrice` 全市場每日資料；需有效 Token 且具 backer／sponsor 權限。先在原 Scanner 設定 Token，再按「補齊歷史資料」。只有一般 Token 仍可能無權限。
   - **匯入的歷史行情**：使用你已取得的完整歷史市場 JSON。下方新增匯入按鈕；接受批次資料，格式如下。
4. 補齊行情後仍需 r18 籌碼快取。搜尋本身只讀本機快取，不呼叫上游。

不同來源的行情分開保存，避免把 FinMind 的分類推估當成官方歷史成分。R19 的舊官方快取與籌碼備份仍可用。換來源後可能需要補齊該來源的行情資料。

## FinMind 的資料契約與限制

使用官方文件的每日全市場接口：`dataset=TaiwanStockPrice&start_date=YYYY-MM-DD`，不帶 `data_id`。Token 透過 Authorization header 傳送，不放在查詢網址或研究報告。

分類資料 `TaiwanStockInfo` 會保留轉板紀錄，舊市場列的 date 停在離開該市場的日期。本版本依這些日期**推估**歷史市場區間，而不是將今天最新 type 套用到所有歷史。分類缺少、日期不涵蓋或同日衝突，整日停止，不刪除不明股票後繼續排名。興櫃排除。

這種推估不等同完整官方 point-in-time 成分檔，仍可能受供應商紀錄修訂／缺漏影響。畫面與匯出報告會標記 `provider-end-date-inference`；需要完全核對歷史市場別時，請用官方資料或帶正確市場別的完整歷史檔。

全市場資料成功抓取一次後，同時存上市與上櫃結果；第二個市場可讀快取，避免同日重複請求。空資料只有在交易日資料能確認休市時才接受。授權／配額失敗與意外空資料不發布成功快取。

官方說明：[FinMind 技術面與全市場日資料](https://finmind.github.io/tutor/TaiwanMarket/Technical/)。權限依供應商帳戶實際回應為準。本次沒有使用者的付費帳戶，因此沒有完成付費來源的真實端到端驗證；未宣稱必定能抓到所有歷史資料。

## 歷史行情 JSON 匯入

一個檔案可包含多日、兩個市場。以下兩種 snapshot 格式擇一：

- 已標準化：最外層 `{ "snapshots": [...] }`。每個元素有 `date`（YYYY-MM-DD）、`market`（twse/tpex）、`closed`、`rows`。每筆 row 有同樣的 `date`、`market`，以及 `code`、`open`、`high`、`low`、`close`、`volume`。成交量單位必須是股；價格未知用 null，不用零補值。非休市日須包含完整該市場的四位數股票，不能只給挑過的 100 檔。
- 原始官方報表：每個元素為 `{ "date": "2024-01-02", "market": "twse", "raw": <該日完整官方 JSON 物件> }`。解析方式與官方來源一致，包含日期／欄位／完整性檢查。

直接使用 snapshot 陣列作為最外層也可以。匯入者須確認檔案涵蓋所選期間及 180 個日曆日暖機；程式可檢查格式、重複、最低資料量與日期，但無法單靠輸入檔證明提供者沒有刪除部分股票。不要把人工抽樣檔視作全市場。

## 驗證

- 原 28 項測試加 7 項新測試，共 35 項。
- 新測試包含：安全頁判斷、停止同源重試、冷卻期間、明確切換 FinMind、快取只讀、Token／會員不足、轉板日期分類、不明市場別中止、空資料不當休市、完整行情匯入，以及 Token 不進 URL／錯誤訊息。
- 分數引擎、訓練／驗證／最終測試與交易成本計算沿用 R19。
- 程式测试不代表已通過真實市場績效驗證；本包不附最佳權重結論。
