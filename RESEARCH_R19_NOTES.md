# R19：每日成交量前 100 名與樣本外策略搜尋

基底：victordogboy/tw-stock-test，r18 commit `ad5d249cbcc0534741574b03442ac1134e8901c8`。
此為增量更新包，由使用者自行上傳；製作過程未寫入 GitHub。

## 更新與使用

1. 把 `update-v1.17.0-r19-top100-oos.zip` 原檔上傳至原 repository 根目錄，不要先解壓縮。
2. 等既有 Apply update package 工作流程及 Cloudflare 部署完成。
3. 開啟原網站 `/strategy-regression-lab.html`，或從首頁新入口進入。
4. 設定期間與費用，按「檢查快取」→「補齊歷史資料」→「搜尋與樣本外驗證」。
5. 匯出研究結果 JSON（含觀察值、成交明細、參數及統計）；匯出資料備份 JSON 可在同版頁面還原。

舊研究頁保留為 `/strategy-regression-lab-r18.html`。正式 Scanner、Detail、V4.4 引擎與 r18 研究快取邏輯不變；新增的是研究頁、隔離的歷史行情 API 及首頁入口。

初次可能需要數百個交易日的官方行情與數百檔入選股票的籌碼，並非只抓今天的 100 檔。可停止續接。資料存在瀏覽器 IndexedDB；既有 Server Cache 仍是 best-effort。換瀏覽器或清除網站資料前請匯出備份。

## 每日排名與資料時点

- 依每個歷史日期的上市／上櫃完整官方行情，按成交**股數**排序，合併市場共 100 檔；也可選單一市場。
- 股票範圍為 1–9 開頭的四位數代號；這是明確的代號篩選，不是經過商品分類資料驗證的普通股全集。0 開頭 ETF、長代號商品不納入。
- 同量以 market:code 排序。先排前百名，再查是否有有效分數；缺資料不拿第 101 名遞補。
- 歷史成分由當日行情決定，不以今日在市名單選樣。入選股票聯集只用於載入資料，不改變過去每日排名。
- 官方回應須能確認日期與 OHLC／股數欄位；錯日期、無法識別、部分市場缺漏、分頁未取全或請求失敗，不發布成功快取。
- 用 180 個日曆日暖機，實際至少需要 90 根有效日 K。IPO／長停牌缺暖機者有覆蓋率紀錄。
- 沿用 r18 真正的 `combineScores` / `entryEngine`：Setup = R.quality，Opportunity = E.opportunity，Entry = E.score，Hold = E.hold。
- 每日分數只使用當日與左側 K 線；籌碼保守延遲至前一交易日以前。融資／法人／當沖最新資料若太舊（逾 4 個額外市場交易日）則不計該日分數。融資缺值不強制轉成零；真實零保留。
- 四項 Δ 均比較前一**市場交易日**，若前日無有效分數則為缺值，不能跨缺漏日計算。
- 資料 API 的历史修訂版本與實際公告時間未完整保存；一日延遲降低時點風險，但不等同完整 point-in-time 資料庫。

## 搜尋與樣本外設計

- 依日期分成 55% 訓練、20% 驗證、25% 最終測試；同一天所有股票屬同一段，沒有隨機拆股票日。
- 最少 160 個訊號交易日。每段最後 26 日不開新倉：最長持有 20 日，加次日成交與退出緩衝；未成交賣單可繼續等待，但不能越過該段結束拿未來价格平倉。
- 候選數可設 64／128／256／512，以固定種子產生；含固定基準與單因子方向候選。
- 進場／出場各搜尋 8 維權重：Setup、Opportunity、Entry、Hold、ΔSetup、ΔOpportunity、ΔEntry、ΔHold。
- 權重可為正負；絕對值總和歸一。絕對分數中心化為 x−50，Δ 轉成 clip(2.5×Δ, −50, 50)。綜合分數 = 50 + Σ(w×中心化特徵)/Σ|w|。
- 進場和出場門檻候選各為 50、55、60、65、70；最長持有 3、5、10、20 日。
- 訓練目標為平均每筆淨報酬 − 0.5×下檔報酬 RMS。先按最少交易數、完整平倉要求過濾，訓練前 8 組進驗證，再只用驗證目標選 1 組。
- 最終測試只評估已鎖定的一組及事前固定基準，不用測試績效挑選策略，也不自動改正式交易規則。
- 這是有限候選搜尋，不能保證全域最佳。若沒有候選通過資料／樣本條件，直接顯示沒有可靠的入選參數。

## 關聯與控制回歸

- 逐訊號日計算截面 Spearman IC，之後取日期平均。每組至少 20 檔。
- 比較四分數與四個 Δ 對 1／3／5／10 日淨報酬的關聯，以及高低五分位平均價差。邊界同分不任意切出價差。
- 報酬定義是 T+1 開盤買，T+h+1 開盤賣，含雙邊費用、賣出稅與滑價。端點無法假設成交的標籤為缺值，可能帶有可成交樣本偏差；標籤統計不等於策略成交紀錄。
- 各段標籤必須在該段內完整實現，使用最大 10 日視窗一致淨化。
- 控制當日報酬、過去 5 日報酬、log 量比與 MA20 乖離。標準化與固定 ridge（懲罰係數 1，不含截距）只在訓練資料估計。
- 分別加一個研究因子，檢查最終測試 MSE 是否改善；不在測試資料重新擬合，不宣稱因果或未校正的顯著性。
- 成交平均報酬的探索性 95% 區間使用依進場日期分群、20 個進場日期的 moving-block bootstrap（300 次）；這不是完整的多重搜尋校正。

## 成交與費用

- T 日收盤訊號最早 T+1 交易所交易日開盤成交，不能買在 T 日收盤。
- 缺價、零成交、一價日 K 不假設能成交。買單僅下一交易日有效，不成交取消；賣單不成交則持續等待。
- 單一 market:code 持倉不重疊。持有後即使掉出前百名仍計算退出與最長持有。
- 每筆淨報酬：
  `sellOpen × (1−slippage) × (1−fee−tax) / [buyOpen × (1+slippage) × (1+fee)] − 1`。
- 預設研究假設：單邊手續費 0.1425%、賣出稅 0.3%、單邊滑價 0.05%，全部可調。這些是可配置的假設，不是對使用者個別費率的判定。
- 有未平倉則該段平均淨報酬不顯示；訓練／驗證有未平倉者不得入選。已平倉平均另外保留在 JSON `closedMean` 供診斷。
- 多檔可以同時持倉，沒有資金限制／部位上限。報表是每筆等名目本金交易統計，**不是**可實現的投資組合報酬、年化或 Sharpe。
- 原始 OHLC 未計股息、配股／分割／除權調整、最低手續費、整張與交易量限制、市場衝擊。這些會影響真實績效，尚不能直接用搜尋結果自動交易。

## 資料來源與配額

歷史上市行情：[TWSE 每日收盤行情](https://www.twse.com.tw/zh/trading/historical/mi-index.html)。
歷史上櫃行情：[TPEx 上櫃股票行情](https://www.tpex.org.tw/zh-tw/mainboard/trading/info/pricing.html)，使用指定日期的 JSON 報表，另保留帶 reportDate 的舊格式相容。
籌碼：沿用 r18 `/api/research/cache/load` 與明確觸發的 `/build`。

新增 `/api/research/market-day?date=YYYY-MM-DD&market=twse|tpex`：GET 僅讀快取，POST 才建置官方歷史行情。排名資料不呼叫 FinMind；補籌碼可能使用 FinMind 配額。回測按鈕只讀 IndexedDB，重新搜尋沒有 FinMind 請求。這裡的配額指資料 API，與 ChatGPT 對話使用量不同。

## 本次驗證與未驗證範圍

- `node --test tests/*.cjs`：28 項測試通過（包括 r18 原 7 項）。涵蓋每日排名、日期驗證、快取失敗保護、次日成交、費用、鎖價／停牌、跨段淨化、未平倉、未來價格不改變訓練結果、真實 r18 引擎重播與 UI cache-only 流程。
- 另做所有新增 JS 語法檢查、ZIP 展開／檔案雜湊比對。
- 已實際取得 2026-09-11 TWSE 歷史報表並經新解析器解析出 1,084 個符合代號範圍的項目。
- TPEx 在本次執行環境回傳 502／逾時；其解析與失敗保護通過固定格式測試，但**尚未完成上櫃實際上游連線驗證**。若部署端也不能取得完整指定日期資料，合併市場研究會停止，可待來源恢復後續接，或明確改選「上市前 100」研究。
- 受本機網址的瀏覽器存取限制，未完成視覺預覽；已以 DOM 替身驗證 UI 流程，以及實際研究 Worker／引擎做完整合成資料管線測試。
- 本包沒有你的完整歷史籌碼快取，**沒有提供真實市場的最佳權重或勝率結論**。部署後補齊資料才能取得實際研究結果。測試 fixtures 全為合成資料，不可視作交易績效。
