# R19.6 白話勝率、手動重測與籌碼分項保存
累積自 r18，保留正式 Scanner/Detail V4.4 分數，不自動套用研究設定。

## 使用
1. 選固定池與原版 V4.4，補齊歷史資料。已成功的舊行情與三類籌碼快取可沿用。
2. 搜尋目標預設「勝率最高（驗證期挑選）」。執行搜尋或以現有資料計算。
3. 第一個結果區顯示驗證／測試勝率、成交筆數、每筆平均扣成本報酬與門檻。
4. 「帶入本次設定」將八個進場及八個出場權重帶進可編輯表格；修改權重、進出場門檻、最長持有1–20交易日，再按「用我的設定重跑勝率」。重測也顯示上次→本次勝率與淨報酬。
5. 手動重測固定使用上次研究的股票池、期間、來源、分數版本與成本，只讀本機資料，零API。若想改資料範圍，先改上方設定並重新搜尋。重測結果/設定可匯出JSON，反覆調整同一測試期間明確標示探索，不能宣稱新樣本外。

## 勝率目標
胜率為扣成本後報酬>0的已平倉交易占比。候選先通過訓練最低交易數與無未平倉條件，再將全部通過者在驗證集評估，以驗證勝率最高選一個；平手以驗證平均淨報酬、候選ID決定。從不以最終測試勝率挑選。只代表本次有限候選中最好，不保證全域最佳或未來胜率。
保留原報酬/下檔風險目標。手動設定不再自動搜尋與改寫，交易數不足、未平倉仍揭露，不假裝通過自動選擇門檻。固定基準仍是原預設設定，非使用者自訂參數。

## 籌碼
新增 /api/research/chip-piece，直接按個股+期間取得融資、法人寬表、當沖；不再呼叫 history/auto 或 TWSE 行情。GET只讀快取，POST才補齊，Token只在Authorization header。每個成功資料集立即存瀏覽器與伺服器快取，下一類失敗不丟棄已成功類別。相同個股／期間只補缺少類別。舊成功資料包可沿用。
狀態表區分尚未下載、來源回覆無資料、配額/方案限制、Token/權限、格式錯誤、來源連線失敗；显示实际有记录日数与行情交易日数/首末日期，不再用三類皆有记录冒充逐日100%完整。判讀分數仍按日期檢查，缺值不補零。
配額/權限錯誤立即停止後續下載；來源累計三次失敗也停止。停止後掃完本機快取並自動部分研究，缺少股票不遞補。空資料成功回應也記錄，其伺服器快取1小時，明確顯示無資料而非成功覆蓋。確切配額歸因仍需帳戶/供應商回應，402可能包含方案限制。
本機分項快取採精確期間鍵：改日期可能重新取得，不是永久增量資料倉庫。伺服器快取可被淘汰；請匯出備份。下載與重測均無法憑空恢復已清除快取。
官方資料集文件 https://finmind.github.io/tutor/TaiwanMarket/Chip/ 與 https://finmind.github.io/tutor/TaiwanMarket/Technical/。

## 驗證與限制
52項測試通過（node --test tests/research*.test.cjs），包含分項成功後遭配額失敗仍保存、續接不重抓成功類別、來源不抓行情、權重正規化/範圍限制、勝率排序與最低交易數門檻、手動表單重跑零網路、實際Worker使用手動參數及原版流程。
嘗試手機尺寸真實瀏覽器驗證，但環境沒有Chromium且下載逾時，未完成瀏覽器視覺驗收；表單與完整Worker已以DOM測試替身及實際Worker腳本驗證。尚未登入使用者FinMind帳戶或部署網址實測，未宣稱真實高勝率結果。
既有固定池今日成分偏差、0050日曆代理、缺除權息處理、無資金上限等研究限制仍適用。勝率高仍可能整體虧損，第一屏同步显示扣成本報酬与樣本數。進階統計折疊，按需展開。
