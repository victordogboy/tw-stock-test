# R19.10 大盤比較
以 R19.9 為前版，累積自 r18；不寫GitHub、不更改參數挑選目標。

## 操作
更新後維持現有股票池、日期和來源，執行一次已有資料研究，然後按「補齊大盤並更新比較」。此按鈕只抓大盤及更新比較，不重新搜尋參數，也不使用 FinMind。
一般補齊資料流程也會嘗試取得大盤；來源失敗不阻止策略計算。本機搜尋/手動重測只讀已存大盤。大盤支援原資料備份匯出/還原。

## 指標與圖表
報告直接顯示策略ROI、大盤ROI、跑贏/落後幾個百分點，以及雙方最大每日收盤回撤。曲線藍色為策略、橘色為加權指數，月報酬表增加大盤月報酬與差距。
基準=T​​AIEX加權價格指數，不含股息/交易成本。採測試開始前一交易日收盤作基準，對齊每個策略淨值日期直到同一終點；假設同本金全額指數曝險，不能把指數直接視為可交易ETF。策略已扣自訂成本、未包含股息/除權息調整，因此此版本是價格指數參考，不能聲稱同口徑總報酬或可實現超額收益。
基準資料不傳入策略因子或候選挑選，不因大盤績效更換最佳參數。股票池/策略日期不因缺大盤日期縮短；缺基準日或任何比較日則明確不比較，沒有向前/向後補值。

## 來源與快取
/api/research/benchmark，GET只讀快取，POST下載；先Yahoo ^TWII，驗證symbol且保留指數小數（不套股票tick）；失敗嘗試TWSE FMTQIK每月報表，驗證回傳月份與日期。兩者都不使用FinMind。只快取成功的資料回應；若日期覆蓋仍不足，報告拒絕比較。伺服器快取可能被淘汰，不是永久資料庫。
官方來源格式實測：https://www.twse.com.tw/exchangeReport/FMTQIK?response=json&date=20260801 ，HTTP200，回傳20260801、21筆日指數、發行量加權股價指數欄位。此驗證不代表使用者Cloudflare出站同樣可連線。

## 驗證
68 tests passed：同本金指數ROI/回撤/超額報酬、月報酬、缺基準日/中間日/重複值拒絕比較、cache-only不呼叫上游、指數精度、官方月份備援，以及所有前版測試。
未於使用者帳戶或真實手機瀏覽器驗證；尚未得知使用者本次策略是否跑贏大盤。既有未還原股價、今日成分、缺漏估值等限制仍適用。尚未改成總報酬指數或ETF淨報酬模型。
