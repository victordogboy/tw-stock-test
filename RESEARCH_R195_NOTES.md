# R19.5 固定池部分資料計算
固定股票池補齊行情或籌碼發生錯誤，停止上游下載後自動以本機成功快取跑探索研究。新增「用已取得資料計算」按鈕，直接零網路執行。使用者按停止不自動啟動研究。
部分模式仍保留原固定池名單與覆蓋率分母：缺行情股票不產生分數、不假設成交，不拿其他股票遞補。
選原版 V4.4 時：已有完整籌碼快取的股票使用原版；其他股票不產生原版分數。若一檔完整籌碼快取都沒有，明確改用純價量版。部分籌碼不補零或拼湊成完整分數。報告保存 requestedScoreMode、實際 scoreMode、missingPrice、missingChips、完整快取股票數、降級理由與停止下載原因。缺籌碼是指不满足 r18 完整快取條件，不代表每個欄位完全沒有資料。
無行情、無0050日曆代理、交易日不足等仍會說明不能計算的原因。每股90根暖機、至少160訊號交易日、時序樣本外、下一交易日成交、成本与退出限制保留。不強行降低最低交易數，不保證一定選出參數。
部分結果明確標示探索性，下載順序與資料可得性可能帶來選樣偏差，不以正向結果宣稱完整策略驗證。這個版本不放寬每日成交量前100模式的完整市場排名要求。
46 tests passed：包括下載來源失敗後僅一次失敗網路嘗試即本機計算、缺行情名單與原分母保留、零籌碼明確價量降級、有完整籌碼保留原版、缺日曆停止、原有Worker樣本外流程等。尚未於使用者部署或帳戶實測。
更新後保留相同瀏覽器/來源/日期；按「用已取得資料計算」。可先匯出資料備份避免瀏覽器清理造成資料遺失。ZIP累積自r18，不寫入GitHub。
