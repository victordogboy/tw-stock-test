# R19.4 統計效能、進度及 Token 輸入
修正標準差 sd：舊版對每個樣本重算整組平均值，時間複雜度 O(n²)；新版只計算平均一次，維持相同樣本標準差公式，O(n)。不修改策略或參數挑選規則。
Worker 新增相關性、控制回歸、驗證與區間估計的進度訊息。
研究頁可直接貼上 FinMind API Token，密碼欄遮蔽顯示，與掃描器共用 twq_finmind_token_v1 本機設定；此按鈕只存瀏覽器，不上傳到伺服器 token-store，不包含於研究匯出。這是 API Token，不是 FinMind 登入帳號密碼，儲存不等於驗證額度。

快取規則未改：同瀏覽器、行情來源與日期區間相同可重用固定池行情；本機搜尋不呼叫上游。換日期區間、來源、裝置或刪除瀏覽器資料可能重抓；伺服器快取可能被清除，不是永久資料庫。先匯出資料備份可還原。純價量選項不抓籌碼；改選原版 V4.4 再補齊，會額外取得融資、法人、當沖等 r18 籌碼，通常每個缺少股票約3–4次以上 FinMind 請求，與行情不同資料集，不是同一次附送。

驗證：42 項測試，新增標準差數值一致與線性讀取次數回歸。249檔/474日/128候選的完整 Worker 合成行情壓力測試另在本機執行；這是計算機制測試，不是使用者真實股票池績效。
